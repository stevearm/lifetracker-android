<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/CouchbaseRedGradientStart"
    android:gravity="center_horizontal"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/splashProgressBar"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:paddingBottom="50px"
        android:scaleType="centerCrop"
        android:src="@drawable/splash" >
    </ImageView>

    <ProgressBar
        android:id="@+id/splashProgressBar"
        style="@android:style/Widget.ProgressBar.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5px"
        android:layout_marginLeft="20px"
        android:layout_marginTop="5px"
        android:layout_toRightOf="@+id/splashProgressMessage"
        android:indeterminate="true" >
    </ProgressBar>

    <TextView
        android:id="@+id/splashProgressMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/imageView1"
        android:layout_centerHorizontal="true"
        android:gravity="center_vertical|center_horizontal"
        android:text="@string/startup_message"
        android:textColor="@android:color/white" >
    </TextView>

</RelativeLayout>